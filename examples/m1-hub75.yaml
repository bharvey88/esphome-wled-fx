# Apollo Automation M-1 (ESP32-S3, 64x64 HUB75 panel) running the display front end.
#
# The engine owns the frame clock, so the display is configured with
# `update_interval: never` and `auto_clear_enabled: false`. wled_fx calls the
# display itself once per frame, which is also what performs the hub75 buffer flip.

esphome:
  name: wled-fx-m1

esp32:
  board: esp32-s3-devkitc-1
  framework:
    type: esp-idf

psram:
  mode: octal
  speed: 80MHz

logger:

# If nothing ever connects to the API, its default reboot_timeout reboots the
# board every 15 minutes. Add `reboot_timeout: 0s` here for a browser only setup.
api:
  encryption:
    key: !secret api_encryption_key

ota:
  - platform: esphome
    password: !secret ota_password

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

external_components:
  - source:
      type: local
      path: ../components

display:
  - platform: hub75
    id: matrix
    board: apollo-automation-m1-rev6
    panel_width: 64
    panel_height: 64
    update_interval: never
    auto_clear_enabled: false
    double_buffer: true

wled_fx:
  id: fx
  display_id: matrix
  update_interval: 23ms
  # A display is a 2D output, so this offers the 64 effects written for a
  # matrix and hides the 159 that only run on a strip. Add
  # `include_1d_effects: true` to offer those here too; they reach the panel
  # through WLED's own 1D to 2D mapping, and this build grows by 59 KB.
  effect: PS Fire
  text: "WLED FX"

select:
  - platform: wled_fx
    wled_fx_id: fx
    type: effect
    name: Effect
  - platform: wled_fx
    wled_fx_id: fx
    type: palette
    name: Palette

number:
  - platform: wled_fx
    wled_fx_id: fx
    type: speed
    name: Speed
  - platform: wled_fx
    wled_fx_id: fx
    type: intensity
    name: Intensity
  - platform: wled_fx
    wled_fx_id: fx
    type: custom1
    name: Custom 1

switch:
  - platform: wled_fx
    wled_fx_id: fx
    type: check1
    name: Check 1

button:
  - platform: template
    name: Next effect
    on_press:
      - wled_fx.next_effect: fx
